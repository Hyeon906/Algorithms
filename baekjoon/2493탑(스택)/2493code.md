```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;
bool chk[10];


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    stack <pair<int,int>> st;
    int N,num,a,k = 0;
    cin>>N;
    for(int i=1; i<=N; i++)
    {
        cin>>num;
        while(!st.empty()&&num>st.top().first)
            st.pop();
        if(st.empty())
            cout<<"0 ";
        else
            cout<<st.top().second<<" ";

        st.push({num,i});

    }
    return 0;
}

```