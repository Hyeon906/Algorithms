```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;

int n,k,ans=0,res=987654321;
int v[100001];
int chk[100001];
void bfs()
{
    queue<pair<int,int>> q;
    q.push({n,0});
    chk[n]=n;
    while(!q.empty())
    {
        int s=q.front().first;
        int e=q.front().second;
        q.pop();
        if(s==k)
        {
            res = e;
            break;

        }
        int arr[3]= {s*2,s+1,s-1};
        for(int i=0; i<3; i++)
        {
            if(arr[i]>=0&&arr[i]<100001&&chk[arr[i]]==-1)
            {
                q.push({arr[i],e+1});
                chk[arr[i]]= s;
            }
        }
    }
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    stack <int> st;
    cin>>n>>k;
    memset(chk,-1,sizeof(chk));
    bfs();
    cout<<res<<endl;
    st.push(k);
    ans = k;
    if(n!=k)
    {
        while(chk[ans]!=n)
        {
            ans = chk[ans];
            st.push(ans);
        }
        cout<<n<<" ";
        while(!st.empty())
        {
            cout<<st.top()<<" ";
            st.pop();
        }

    }
    else
        cout<<n<<"\n";
    return 0;
}

```