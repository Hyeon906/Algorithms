```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;

long long A,B,res=0;
void bfs()
{
    queue<pair<long long,long long>> q;
    q.push({A,1});
    while(!q.empty())
    {
        long long s=q.front().first;
        long long e=q.front().second;
        q.pop();
        if(s==B)
        {
            res = e;
            break;
        }
        long long arr[2]= {s*2,s*10+1};
        for(int i=0; i<2; i++)
        {
            if(arr[i]<=B)
                q.push({arr[i],e+1});
        }
    }
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>A>>B;
    bfs();
    if(res==0)
        cout<<"-1"<<endl;
    else
        cout<<res<<endl;
    return 0;
}

```