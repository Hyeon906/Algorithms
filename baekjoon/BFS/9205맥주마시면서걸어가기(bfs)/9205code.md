```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;

struct Data
{
    int o,p,l;
};
int t,n,s1,s2,c1,c2,e1,e2;
int cnt = 0;
bool chk[101];
//int dx[4] = { -1,0,1,0 };
//int dy[4] = { 0,-1,0,1 };
vector <pair<int,int>> v(101);

void dfs(int a,int b)
{
    if(abs(a-e1)+abs(b-e2)<=1000)
    {
        cnt++;
        return;
    }
    for(int i=0;i<n;i++)
    {
        if(abs(v[i].first-a)+abs(v[i].second-b)<=1000&&!chk[i])
        {
            chk[i]=true;
            //cout<<v[i].first<<" "<<v[i].second<<endl;
            dfs(v[i].first,v[i].second);
        }
    }
}



int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>t;
    for(int i=0;i<t;i++)
    {
        cin>>n;
        cin>>s1>>s2;
        for(int j=0;j<n;j++)
            cin>>v[j].first>>v[j].second;
        cin>>e1>>e2;
        dfs(s1,s2);
        if(cnt>0)
            cout<<"happy"<<endl;
        else
            cout<<"sad"<<endl;
        cnt = 0;
        memset(chk,false,sizeof(chk));
    }
    return 0;
}

```