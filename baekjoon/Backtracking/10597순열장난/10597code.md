```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;

int N,ct = 0,res=0, s=0;
string str;
vector <int> v;
int arr[101];
bool chk[101];

void dfs(int a)
{
    if(a==str.size())
    {
        for(int i=0;i<v.size(); i++)
            cout<<v[i]<<" ";
        cout<<endl;
        exit(0);
    }
    int k = 0;
    for(int i=a; i<=a+1; i++)
    {
        k=k*10+arr[i];
        if(k>s||chk[k])
            continue;
        chk[k] = true;
        v.push_back(k);
        dfs(i+1);
        chk[k] = false;
        v.pop_back();
    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int a,b;
    cin>>str;
    for(int i=0; i<str.size(); i++)
        arr[i]= int(str[i]-'0');
    s = str.size();
    if(str.size()>9)
        s = (str.size()-9)/2+9;
    dfs(0);

}

```