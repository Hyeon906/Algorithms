```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;

int N, M, num;
int arr[10];
int v[10];
bool chk[10];

void dfs(int a, int b)
{
    if (a == M)
    {
        for (int i = 0; i < M; i++)
            cout << v[i] << " ";
        cout << "\n";
        return;
    }
    for (int i = 0; i < N; i++)
    {
        
            if (arr[i] != arr[i + 1])
            {
                //chk[i] = true;
                v[a] = arr[i];
                dfs(a + 1, i + 1);
                //chk[i] = false;
            }

    }
}   

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin >> N >> M;
    for (int i = 0; i < N; i++)
        cin >> arr[i];
    sort(arr, arr + N);
    dfs(0, 0);
    return 0;
}

```