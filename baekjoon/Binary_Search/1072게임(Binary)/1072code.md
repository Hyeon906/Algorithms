```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;
vector <long long> v;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    long long X,Y,Z,s = 0,e = 1000000000,mid,mx = 0,k;
    cin>>X>>Y;
    Z = (Y*100)/X;
    if(X==Y||Z==99)
        cout<<"-1"<<endl;
    else
    {
        while(s<=e)
        {
            mid = (s+e)/2;
            k = ((Y+mid)*100)/(X+mid);
            if(k>Z)
            {
                e = mid-1;
                mx = mid;
            }
            else
                s = mid+1;
        }
        cout<<mx<<endl;
    }
    return 0;
}
```