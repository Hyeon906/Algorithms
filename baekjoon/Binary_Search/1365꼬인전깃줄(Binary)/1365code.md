```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;
int N,num,A,B;
int k, mx = 0;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    vector <int> v;
    cin>>N;
    for(int i=0;i<N;i++)
    {
        cin>>num;
        if(i==0)
            v.push_back(num);
        else
        {
            if(v.back()<num)
                v.push_back(num);
            else
            {
                int p = lower_bound(v.begin(),v.end(),num)-v.begin();
                v[p] = num;
            }
        }
    }
    cout<<N-v.size()<<endl;
    return 0;
}

```