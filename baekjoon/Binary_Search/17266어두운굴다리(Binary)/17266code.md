```
#include <bits/stdc++.h>
using namespace std;
int N,M,x,e=100001,s = 0,m;
int arr[100001];
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>N>>M;
    for(int i=0;i<M;i++)
        cin>>arr[i];
    s=1;e=N;
    while(s<=e)
    {
        int k = 0;
        int mid = (s+e)/2;
        int cur = 0;
        for(int i=0;i<M;i++)
        {
            if (cur < arr[i] - mid) break;
            cur = arr[i] + mid;
        }

        if(cur>=N){
            e = mid-1;
            m = mid;
        }
        else s = mid+1;
        //cout<<s<<" "<<e<<endl;
    }
    cout<<m<<endl;
    return 0;
}

```