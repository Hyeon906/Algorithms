```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;

int N,ct = 0;
bool chk_t[16];
bool chk_l[16];
bool chk_pd[32];
bool chk_md[32];
//bool chk[16][16];


void dfs(int k)
{
    if(k==N)
    {
        ct++;
        return;
    }
    for(int i=0; i<N; i++)
    {
        if(chk_l[i]||chk_pd[i+k]||chk_md[N+(i-k)])
            continue;
        chk_l[i] = chk_md[N+(i-k)] = chk_pd[i+k] = true;
        //cout<<i<<" "<<k<<endl;
        dfs(k+1);
        chk_l[i] = chk_md[N+(i-k)] = chk_pd[i+k] = false;
    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int a,b;
    cin>>N;
    dfs(0);
    cout<<ct<<endl;
}

```