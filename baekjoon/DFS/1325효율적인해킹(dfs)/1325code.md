```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;

int N, M;
int A,B;
int arr[100001];
int ct = 0; int mx = 0; int mx1 = 0;
int chk[100001];
vector <int> v[100001];
queue <int> q;

void dfs(int k)
{
    chk[k]=1;
    ct++;
    for(int i=0; i<v[k].size(); i++)
    {
        int st = v[k][i];
        if(chk[st])
            continue;
        dfs(st);
    }
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>N>>M;
    for(int i = 0;i<M;i++)
    {
        cin>>A>>B;
        v[B].push_back(A);
    }
    for(int j=1;j<=N;j++)
    {
        ct = 0;
        dfs(j);
        arr[j] = ct;
        memset(chk,0,sizeof(chk));
    }
     for(int i=1; i<=N; i++)
     {
        if(arr[i]>mx)
        {
            mx = arr[i];
            q = queue <int>();
            q.push(i);
        }
        else if(arr[i]==mx)
            q.push(i);
     }
     while(!q.empty())
     {
         cout<<q.front()<<" ";
         q.pop();
     }
}

```