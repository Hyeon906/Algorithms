```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;
int arr[1001];
int dp[1001];
int num,N,ct = 0,mx = 0;



int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>N;
    //memset(arr,-1,sizeof(arr));
    for(int i=0;i<N;i++)
        cin>>arr[i];
    for(int i=0;i<N;i++)
    {
        dp[i] = 1;
        for(int j=0;j<i;j++)
        {
            if(arr[i]>arr[j])
                dp[i] = max(dp[i],dp[j]+1);
        }
        mx = max(mx,dp[i]);
    }
    cout<<mx;
    return 0;
}

```