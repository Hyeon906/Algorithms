```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;
int N,num,mx = 0;
vector <int> v;
int dp[1000001];
int arr[1000001];
int re(int a)
{
    if(a==1) return 0;
    if(dp[a]!=0) return dp[a];
    int res = re(a-1)+1;
    arr[a] = a-1;
    if(a%2==0){
        if(res>re(a/2)+1)
        {
            res = re(a/2)+1;
            arr[a] = a/2;
        }
    }
    if(a%3==0)
    {
        if(res>re(a/3)+1)
        {
            res = re(a/3)+1;
            arr[a] = a/3;
        }
    }
    return dp[a] = res;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>N;
    num = N;
    int k = re(N);
    cout<<k<<endl;
    cout<<N<<" ";
    while(num>1)
    {
        cout<<arr[num]<<" ";
        num = arr[num];
    }
    return 0;
}
```