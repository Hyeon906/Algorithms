```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;
//int arr[100001];
int dp[100001];
int num,N,ct = 0,mx = 0;

int dfs(int a)
{
    if(a==3||a<=1) return 987654321;
    if(dp[a]>0) return dp[a];
    if(a==2 || a==5) return dp[a]=1;
    return dp[a] = min(dfs(a-2)+1,dfs(a-5)+1);
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>N;
    //memset(dp,-1,sizeof(dp));
    ct = dfs(N);
    if(ct==987654321)
        cout<<"-1";
    else
        cout<<ct;
    return 0;
}
```