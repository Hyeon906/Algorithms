```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;
int N;
int sum = 0, mx = -199999999, same_sum = 0;
int arr[100001];
int dp[100001];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>N;
    for(int i=0;i<N;i++)
    {
        cin>>arr[i];
        if(i==0)
        {
            dp[i] = arr[i];
        }
        else
            dp[i]= max(arr[i],dp[i-1]+arr[i]);
        mx = max(dp[i],mx);
    }
    cout<<mx<<endl;
    return 0;
}

```