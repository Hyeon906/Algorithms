```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;
int arr[301];
int dp[301];
//int dp1[301];
int num,N,ct,mx = 0;

int dfs(int a)
{
    if(dp[a]) return dp[a];
    if(a<=1)
        return dp[a] = arr[a];
    else if(a==2)
        return dp[a] = arr[1]+arr[2];
    else
        return dp[a] = max(dfs(a-3)+arr[a-1],dfs(a-2))+arr[a];

}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>N;
    //memset(arr,-1,sizeof(arr));
    for(int i=1;i<=N;i++)
        cin>>arr[i];
    cout<<dfs(N);
    return 0;
}
```
