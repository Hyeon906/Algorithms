```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;
int dp[1000001];
bool chk[4];
int num,ct;

void dfs(int b)
{
    if (b == num)
    {
        ct++;
        return;
    }

    for (int i = 1; i <= 3; i++)
    {
        if(b+i<=num)
            dfs(b+i);
    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int N;
    cin>>N;
    for(int i=0;i<N;i++)
    {
        ct = 0;
        cin>>num;
        dfs(0);
        cout<<ct<<endl;
        memset(dp,0,sizeof(dp));
    }
    return 0;
}

```