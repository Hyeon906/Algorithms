```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;
vector <int> v[200001];
priority_queue <int> pq;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int N,tree,twos = 0,sum = 0;
    cin>>N;
    for(int i=0;i<N;i++)
    {
        cin>>tree;
        sum+=tree;
        twos+=tree/2;
    }
    if(sum%3==0&&twos>=sum/3)
        cout<<"YES"<<endl;
    else
        cout<<"NO"<<endl;
    return 0;
}

```