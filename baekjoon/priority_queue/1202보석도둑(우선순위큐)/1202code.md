```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;


int N,K,M,V,C,mx = 0;
long long sum = 0;
bool chk[3000001];
vector <pair<int,int>> v;
priority_queue <int> pq;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int k = 0;
    cin>>N>>K;int arr[K];
    for(int i=0; i<N; i++)
    {
        cin>>M>>V;
        v.push_back({M,V});
    }
    sort(v.begin(),v.end());
    for(int i=0; i<K; i++)
        cin>>arr[i];
    sort(arr,arr+K);
    for(int i=0;i<K;i++)
    {
        while(arr[i]>=v[k].first&&k<N)
        {
            pq.push(v[k].second);
            k++;
        }
        if(!pq.empty())
        {
            sum+=pq.top();
            pq.pop();
        }
    }
    cout<<sum<<endl;
    return 0;
}

```
