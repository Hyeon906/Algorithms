```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;
bool chk[10];


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    stack <int> st;
    //stack <int> hp;
    stack <int> as;
    int N,num;
    cin>>N;
    int arr[N+1];
    for(int i=0;i<N;i++)
        cin>>arr[i];
    for(int i=N-1;i>=0;i--)
    {
        while(!st.empty()&&arr[i]>=st.top())
            st.pop();
        if(st.empty())
            as.push(-1);
        else
            as.push(st.top());
        st.push(arr[i]);
    }
    while(!as.empty())
    {
        cout<<as.top()<<" ";
        as.pop();
    }
    return 0;
}

```