```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
using namespace std;
int arr[1000001];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int N,K,i,g,x,mx,sum,a,mx1;
    mx = 0;sum = 0; mx1 = 0;
    cin>>N>>K;
    for(i=1;i<=N;i++)
    {
        cin>>g>>x;
        arr[x] = g;
        if(mx<x)
            mx = x;
    }

    for(i=0;i<=1000000;i++)
    {
        if(i>=2*K+1)
            sum -= arr[i-(2*K+1)];
        sum+=arr[i];
        if(sum>=mx1)
            mx1 = sum;
    }
    cout<<mx1<<endl;
    return 0;
}

```
