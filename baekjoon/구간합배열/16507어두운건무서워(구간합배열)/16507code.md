```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
using namespace std;
int arr[1001][1001];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int R,C,Q,y1,y2,x1,x2,i,j,num;
    cin>>R>>C>>Q;
    for(i=0;i<R;i++)
    {
        for(j=0;j<C;j++)
        {
            cin>>num;
            arr[i+1][j+1]=arr[i][j+1]+arr[i+1][j]-arr[i][j]+num;
        }
    }
    for(i=0;i<Q;i++)
    {
        cin>>y1>>x1>>y2>>x2;
        int N = (y2-y1+1)*(x2-x1+1);
        int sum = arr[y2][x2]-arr[y1-1][x2]-arr[y2][x1-1]+arr[y1-1][x1-1];
        cout<<sum/N<<"\n";
    }
    return 0;
}
```
