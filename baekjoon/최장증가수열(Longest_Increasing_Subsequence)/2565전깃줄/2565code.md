```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;
int N,num,A,B;
int k, mx = 0;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    vector <pair<int,int>> v;
    cin>>N;
    for(int i=0;i<N;i++)
    {
        cin>>A>>B;
        v.push_back({A,B});
    }
    sort(v.begin(),v.end());
    vector <int> v1;
    v1.push_back(v[0].second);
    for(int i=1;i<v.size();i++)
    {
        if(v[i].second>v1.back())
            v1.push_back(v[i].second);
        else
        {
            int p = lower_bound(v1.begin(),v1.end(),v[i].second) - v1.begin();
            v1[p] = v[i].second;
        }
    }
    cout<<N-v1.size()<<endl;

    return 0;
}
```