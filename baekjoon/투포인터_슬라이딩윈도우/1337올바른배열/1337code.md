```
#include <bits/stdc++.h>
using namespace std;
int N,M,K,a,b,ans = 0,mx=0,le,ri;
vector <int> adj[100001];
vector <int> degree1(100001);
vector <int> degree2(100001);
long long arr[51];
priority_queue <int> pq;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>N;
    for(int i=0;i<N;i++)
        cin>>arr[i];
    sort(arr,arr+N);
    for(int i=0;i<N;i++)
    {
        le = i; ri= N-1;
        while(left<right)
        {
            if(arr[ri]-arr[le]>=5){
                ri--;
                continue;
            }
            mx = max(mx,ri-le);
            break;
        }
    }
    cout<<4-mx<<endl;
    return 0;
}

```