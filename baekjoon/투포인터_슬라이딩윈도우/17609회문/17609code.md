```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
#include <cstring>
using namespace std;

int sameword(string word)
{
    for(int i=0;i<word.size();i++)
    {
        if(word[i]!=word[word.size()-1-i])
            return i;
    }
    return word.size()-1;
}


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);
    string a;
    int N,ch,t=0,k,q=0;
    char mem;
    cin>>N;
    for(int i=0; i<N; i++)
    {
        cin>>a;
        k = sameword(a);
        if(k==a.size()-1)
            cout<<"0"<<endl;
        else if(k<a.size()-1)
        {
            string b="";
            string c="";
            for(int i=0;i<a.size();i++)
            {
                if(i!=k)
                    b+=a[i];
                if(i!=a.size()-k-1)
                    c+=a[i];
            }
            t = sameword(b);
            q = sameword(c);
            if(t==b.size()-1||q==c.size()-1)
                cout<<"1"<<endl;
            else
                cout<<"2"<<endl;
        }

    }

}

```