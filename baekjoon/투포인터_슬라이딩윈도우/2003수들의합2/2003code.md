```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
using namespace std;
long long arr[30001];


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    long long M,num,N,i,j,k,a;
    k =0 ;a = 0;
    cin>>N>>M;
    for(i=1;i<=N;i++)
    {
        cin>>num;
        arr[i]=arr[i-1] + num;
    }
    for(i=1;i<=N;i++)
    {
        if(arr[i]<M)
            continue;
        else if(arr[i]==M)
            k++;
        else
        {
            while(arr[i]-arr[a]>M)
                a++;
            if(M==arr[i]-arr[a])
            {
                
                k++;
            }
            a = 0;
        }
    }
    cout<<k<<"\n";
    return 0;
}

```