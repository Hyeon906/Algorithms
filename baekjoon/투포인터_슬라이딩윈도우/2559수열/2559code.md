```
#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
#include <iomanip>
#include <queue>
#include <vector>
using namespace std;
int arr[100001];


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int M,num,N,i,big,a;
    big =-10000000;a = 0;
    cin>>N>>M;
    for(i=1;i<=N;i++)
    {
        cin>>num;
        arr[i]=arr[i-1] + num;
    }
    for(i=M;i<=N;i++)
    {
        //cout<<arr[i]-arr[a]<<endl;
        if(big<=arr[i]-arr[a])
            big=arr[i]-arr[a];
        a++;
    }
    cout<<big<<"\n";
    return 0;
}
```