```
#include <bits/stdc++.h>
using namespace std;
long long N,le,ri,k = 0;
long long m_ri,m_le,m_mi,mini;
vector <long long> v(20002);

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>N;
    for(int i=0;i<N;i++)
        cin>>v[i];
    sort(v.begin(),v.begin()+N);
    for(int i=0;i<N-2;i++)
    {
        le = i+1; ri = N-1;
        while(le<ri){
            if(v[le]+v[ri]+v[i]>0) ri--;
            else{
                if(v[le]+v[ri]+v[i]==0)
                {
                    int p = lower_bound(v.begin(),v.begin()+N,v[ri])-v.begin();
                    if(p<=le) k+=(ri-le);
                    else k+=(ri-(p-1));
                }
                le++;
            }
        }
    }
    cout<<k<<endl;
    return 0;
}

```